from os import name
from django.contrib import admin
from django.urls import path, include

from .views import *

app_name = "user_activity"

urlpatterns = [
    # path('', login_request, name="get_all_post"),
    
]